from enum import Enum
class Variant(Enum):
    """
    Enum class representing different variants of a product.
    Each variant has a unique identifier and a descriptive name.
    """

    VARIANT_A = "High End"
    VARIANT_B = "Mid Range"
    VARIANT_C = "Entry Level"

    