[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "automobileanalysisapp"
version = "1.0.0"
description = "Automated vehicle management application"
requires-python = ">=3.10"

dependencies = [
  
  "pytest>=7.0.0",
  "Faker>=18.0.0"
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]