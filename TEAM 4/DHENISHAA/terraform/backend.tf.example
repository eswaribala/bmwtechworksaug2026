terraform {
  backend "s3" {
    bucket = "your-terraform-state-bucket"
    key    = "bmw-serverless-data-lake/dev/terraform.tfstate"
    region = "us-east-1"
    encrypt = true
    dynamodb_table = "terraform-locks"
  }
}
