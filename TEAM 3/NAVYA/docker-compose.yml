services:

  # ==========================================================
  # BMW ENTERPRISE ETL - REACT FRONTEND
  # ==========================================================

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile

    container_name: bmw-etl-frontend

    ports:
      - "3000:80"

    restart: unless-stopped