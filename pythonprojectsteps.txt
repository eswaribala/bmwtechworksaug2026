Create venv
python –m venv vehicleenv
#activate
vehicleenv\Scripts\activate
If it is bash shell in github codespaces
source vehicleenv/bin/activate
Ctrl+shift+p open folder for specific
#install dependencies from pyproject.toml
pip install -e .

python -m pip install pytest
# run pytest
pytest
